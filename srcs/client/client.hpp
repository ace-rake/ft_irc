#ifndef CLIENT_HPP
# define CLIENT_HPP

// Custom class: client
class	client
{
	public:
		// Constructor
		client	(	void	);

		// Copy constructor
		client	(	const client& other	);

		// Copy assignment operator overload
		client& operator = (const client& other);

		// Destructor
		~client	(void);

		// Getters and Setter

	protected:
		/*_*/;

	private:
		/*_*/;
};

#endif

